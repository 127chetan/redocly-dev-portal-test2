type: object
properties:
  receiptFormat:
    type: string
    description: Value of tag E1.DF8206 from NEXO config.
  extAppLabel:
    type: string
    description: Value of tag E6.DF8211 or E6.DF8212 (Online or Offline TX printout).
  preNotification:
    type: string
    description: Value of tag E6.DF8214 info form NEXO config.
  mandate:
    type: string
    description: Value of tag E6.DF8215 info from NEXO config.
  creditorId:
    type: string
    description: Value of tag E6.DF8216 info from NEXO config.
  mandateId:
    type: string
    description: >-
      Generated by SPA according to ELV – TBD: use Trace number instead of
      receipt number.
  iban:
    type: string
    description: Received in ICC related data from IPG (tag 5F53).
  isMerchantForced:
    type: boolean
    description: >-
      Defines if the corresponding SEPA ELV TX was forced by the merchant
      (override defined limit)
