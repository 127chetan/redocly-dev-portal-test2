type: object
properties:
  printed:
    type: integer
    description: number of printed line items deleted
    format: int64
  unprinted:
    type: integer
    description: number of unprinted line items deleted
    format: int64
  price:
    type: integer
    description: 'sum of price of all the items, in cents of merchant currency.'
    format: int64
  priceWithQuantity:
    type: integer
    description: >-
      sum of price of all the items, in cents of merchant currency multiplied by
      quantity.
    format: int64
  priceWithQuantityAndModifiers:
    type: integer
    description: >-
      sum of price of all the items multiplied with quantity and includes
      modifiers.
    format: int64
  deletedOrders:
    type: integer
    description: number of orders deleted
    format: int64
  deletedItems:
    type: integer
    description: number of line items deleted
    format: int64
  chartSummary:
    description: summary of the chart data
    $ref: ./VoidedItemsChartSummary.yaml
