type: object
properties:
  developerAppId:
    type: string
    description: >-
      The uuid from the developer application.  This is typically populated and
      used only on the back end.
  applicationName:
    type: string
    description: The name of the developer application.
  applicationID:
    type: string
    description: A string representing an application
  applicationVersion:
    type: string
    description: 'A string representing a semanticversion.  See http://semver.org/'
  sourceSDK:
    type: string
    description: A string representing a SDK
  sourceSDKVersion:
    type: string
    description: 'A string representing a semanticversion.  See http://semver.org/'
