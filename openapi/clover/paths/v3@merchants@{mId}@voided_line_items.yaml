get:
  tags:
    - ORDERS
  summary: Get order line items that were recorded after a void
  description: ''
  operationId: lineitem.GetVoidedOrderLineItems
  parameters:
    - name: mId
      in: path
      description: Merchant Id
      required: true
      schema:
        type: string
    - name: filter
      in: query
      description: >-
        Filter fields: [reason, quantity, orderId, revenueClassName, deleteType,
        isRevenue, approvedByUuid, sortByName, printed, orderType.id, price,
        name, showInReporting, lineItemUuid, id, deletedTime]
      schema:
        type: string
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../components/schemas/VoidedLineItem.yaml
  deprecated: false
