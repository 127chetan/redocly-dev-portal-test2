put:
  tags:
    - INVENTORY
  summary: >-
    Reorder modifiers in the modifier group based on the ordering of the
    incoming modifier id list. Remove deleted modifiers. Add missing modifiers.
  description: ''
  operationId: modifier.ReorderModifiers
  parameters:
    - name: mId
      in: path
      description: Merchant Id
      required: true
      schema:
        type: string
    - name: modGroupId
      in: path
      description: Modifier Group Id
      required: true
      schema:
        type: string
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/IdArray.yaml
  responses:
    '200':
      description: ''
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Reference.yaml
  deprecated: false
